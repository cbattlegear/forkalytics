# Database Configuration
POSTGRES_USER=forkalytics
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=forkalytics

# Mastodon Configuration
# The Mastodon instance URL to monitor (e.g., https://mastodon.social)
MASTODON_INSTANCE=https://mastodon.social
# OAuth access token with read:statuses scope
# To get a token:
# 1. Go to your Mastodon instance -> Preferences -> Development -> New Application
# 2. Name it "Forkalytics", select read:statuses scope
# 3. Copy the access token
MASTODON_ACCESS_TOKEN=your_mastodon_access_token_here

# OpenAI Configuration
# Used for sentiment analysis and trend summarization
# You can use EITHER standard OpenAI OR Azure OpenAI (not both)

# Option 1: Standard OpenAI
OPENAI_API_KEY=your_openai_api_key_here
# OPENAI_MODEL=gpt-4o-mini  # Optional, defaults to gpt-4o-mini

# Option 2: Azure OpenAI (uncomment and fill in to use Azure instead)
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
# AZURE_OPENAI_API_KEY=your_azure_openai_api_key_here
# AZURE_OPENAI_DEPLOYMENT=your-deployment-name  # e.g., gpt-4o-mini
# AZURE_OPENAI_API_VERSION=2024-02-15-preview  # Optional, defaults to 2024-02-15-preview
